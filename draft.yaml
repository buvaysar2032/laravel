models:
  Post:
    title: string:255
    content: longtext
    published_at: nullable timestamp
    text_id: id:text

controllers:
  Post:
    index:
      query: all
      render: post.index with:posts

    store:
      validate: title, content, text_id
      save: post
      redirect: posts.index
